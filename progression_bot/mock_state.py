"""Mock data has moved to JSON fixtures.

See `fixtures/mock_state.json`.

TODO(student):
- Create domain models and a loader that reads fixtures/state from JSON.
- Use that data to implement `/status`, `/last14`, `/heatmap`, `/plan`.
"""
